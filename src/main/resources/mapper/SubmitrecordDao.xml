<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.xchuiao.projectprocedurems.dao.SubmitrecordDao">

    <resultMap type="top.xchuiao.projectprocedurems.entity.Submitrecord" id="SubmitrecordMap">
        <result property="proId" column="pro_id" jdbcType="VARCHAR"/>
        <result property="staId" column="sta_id" jdbcType="VARCHAR"/>
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="intro" column="intro" jdbcType="VARCHAR"/>
        <result property="submitTime" column="submit_time" jdbcType="TIMESTAMP"/>
        <result property="directorAudit" column="director_audit" jdbcType="INTEGER"/>
        <result property="directorComment" column="director_comment" jdbcType="VARCHAR"/>
        <result property="clientComment" column="client_comment" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="SubmitrecordMap">
        select
          pro_id, sta_id, id, intro, submit_time, director_audit, director_comment, client_comment
        from project_procedure_ms.submitrecord
        where pro_id = #{proId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SubmitrecordMap">
        select
          pro_id, sta_id, id, intro, submit_time, director_audit, director_comment, client_comment
        from project_procedure_ms.submitrecord
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="SubmitrecordMap">
        select
          pro_id, sta_id, id, intro, submit_time, director_audit, director_comment, client_comment
        from project_procedure_ms.submitrecord
        <where>
            <if test="proId != null and proId != ''">
                and pro_id = #{proId}
            </if>
            <if test="staId != null and staId != ''">
                and sta_id = #{staId}
            </if>
            <if test="id != null and id != ''">
                and id = #{id}
            </if>
            <if test="intro != null and intro != ''">
                and intro = #{intro}
            </if>
            <if test="submitTime != null">
                and submit_time = #{submitTime}
            </if>
            <if test="directorAudit != null">
                and director_audit = #{directorAudit}
            </if>
            <if test="directorComment != null and directorComment != ''">
                and director_comment = #{directorComment}
            </if>
            <if test="clientComment != null and clientComment != ''">
                and client_comment = #{clientComment}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="proId" useGeneratedKeys="true">
        insert into project_procedure_ms.submitrecord(intro, submit_time, director_audit, director_comment, client_comment)
        values (#{intro}, #{submitTime}, #{directorAudit}, #{directorComment}, #{clientComment})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update project_procedure_ms.submitrecord
        <set>
            <if test="intro != null and intro != ''">
                intro = #{intro},
            </if>
            <if test="submitTime != null">
                submit_time = #{submitTime},
            </if>
            <if test="directorAudit != null">
                director_audit = #{directorAudit},
            </if>
            <if test="directorComment != null and directorComment != ''">
                director_comment = #{directorComment},
            </if>
            <if test="clientComment != null and clientComment != ''">
                client_comment = #{clientComment},
            </if>
        </set>
        where pro_id = #{proId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from project_procedure_ms.submitrecord where pro_id = #{proId}
    </delete>

</mapper>