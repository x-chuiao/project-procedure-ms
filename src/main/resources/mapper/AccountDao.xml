<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.xchuiao.projectprocedurems.dao.AccountDao">

    <resultMap type="top.xchuiao.projectprocedurems.entity.Account" id="AccountMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="rolId" column="rol_id" jdbcType="VARCHAR"/>
        <result property="pwd" column="pwd" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="AccountMap">
        select
          id, rol_id, pwd
        from project_procedure_ms.account
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="AccountMap">
        select
          id, rol_id, pwd
        from project_procedure_ms.account
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="AccountMap">
        select
          id, rol_id, pwd
        from project_procedure_ms.account
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into project_procedure_ms.account(rol_id, pwd)
        values (#{rolId}, #{pwd})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update project_procedure_ms.account
        <set>
            <if test="rolId != null and rolId != ''">
                rol_id = #{rolId},
            </if>
            <if test="pwd != null and pwd != ''">
                pwd = #{pwd},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from project_procedure_ms.account where id = #{id}
    </delete>

</mapper>