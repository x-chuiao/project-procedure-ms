<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.xchuiao.projectprocedurems.dao.PositionDao">

    <resultMap type="top.xchuiao.projectprocedurems.entity.Position" id="PositionMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PositionMap">
        select
          id, name
        from project_procedure_ms.position
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PositionMap">
        select
          id, name
        from project_procedure_ms.position
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="PositionMap">
        select
          id, name
        from project_procedure_ms.position
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" >
        insert into project_procedure_ms.position(id, name)
        values (#{id},#{name})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update project_procedure_ms.position
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from project_procedure_ms.position where id = #{id}
    </delete>

</mapper>